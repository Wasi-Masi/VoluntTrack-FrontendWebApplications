import { CreateActivityEntity } from './create-activity.entity';

describe('CreateActivityEntity', () => {
  it('should create an instance', () => {
    expect(new CreateActivityEntity()).toBeTruthy();
  });
});
