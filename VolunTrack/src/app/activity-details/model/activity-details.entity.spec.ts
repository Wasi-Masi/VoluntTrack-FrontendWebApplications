import { ActivityDetailsEntity } from './activity-details.entity';

describe('ActivityDetailsEntity', () => {
  it('should create an instance', () => {
    expect(new ActivityDetailsEntity()).toBeTruthy();
  });
});
