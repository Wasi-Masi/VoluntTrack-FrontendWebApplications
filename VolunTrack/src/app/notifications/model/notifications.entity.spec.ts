import { NotificationsEntity } from './notifications.entity';

describe('NotificationsEntity', () => {
  it('should create an instance', () => {
    expect(new NotificationsEntity()).toBeTruthy();
  });
});
